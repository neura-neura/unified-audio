<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="UnifiedAudio.Views.EditProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnifiedAudio.Views">

    <Grid Padding="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
            <Button
                x:Name="BackButton"
                Style="{StaticResource CompactIconButtonStyle}"
                Click="BackButton_Click">
                <FontIcon FontSize="16" Glyph="&#xE72B;" />
            </Button>
            <TextBlock
                x:Name="TitleText"
                Style="{StaticResource PageTitleStyle}"
                VerticalAlignment="Center" />
        </StackPanel>

        <ScrollViewer
            Grid.Row="1"
            Margin="0,8,0,8"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Disabled">
            <StackPanel MaxWidth="760" Spacing="10">
                <StackPanel>
                    <TextBlock x:Name="NameLabel" Style="{StaticResource FieldLabelStyle}" />
                    <TextBox x:Name="NameBox" />
                </StackPanel>

                <StackPanel>
                    <TextBlock x:Name="IconLabel" Style="{StaticResource FieldLabelStyle}" />
                    <GridView
                        x:Name="IconGrid"
                        Padding="0"
                        Margin="0,-4,0,0"
                        HorizontalAlignment="Left"
                        IsItemClickEnabled="True"
                        SelectionMode="Single"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Disabled">
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="Margin" Value="0,0,8,4" />
                                <Setter Property="MinWidth" Value="0" />
                                <Setter Property="MinHeight" Value="0" />
                                <Setter Property="Padding" Value="0" />
                            </Style>
                        </GridView.ItemContainerStyle>
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Width="68" Padding="2" Spacing="2">
                                    <Border
                                        Width="28"
                                        Height="28"
                                        HorizontalAlignment="Center"
                                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                        CornerRadius="{StaticResource IconTileCornerRadius}">
                                        <FontIcon
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontSize="14"
                                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                            Glyph="{Binding Glyph}" />
                                    </Border>
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Opacity="0.82"
                                        Text="{Binding Label}" />
                                </StackPanel>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                </StackPanel>

                <StackPanel>
                    <TextBlock x:Name="OutputLabel" Style="{StaticResource FieldLabelStyle}" />
                    <ComboBox x:Name="OutputBox" HorizontalAlignment="Stretch" />
                    <TextBlock x:Name="OutputWarning" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="Collapsed" />
                </StackPanel>

                <StackPanel x:Name="GlobalInputSummaryPanel" Spacing="4" Visibility="Collapsed">
                    <TextBlock x:Name="GlobalInputSummary" Style="{StaticResource SectionBodyStyle}" TextWrapping="Wrap" />
                    <Button x:Name="ConfigureGlobalInputButton" HorizontalAlignment="Left" Click="ConfigureGlobalInputButton_Click" />
                </StackPanel>

                <TextBlock x:Name="LegacyInputNotice" Style="{StaticResource SectionBodyStyle}" TextWrapping="Wrap" Visibility="Collapsed" />

                <StackPanel>
                    <TextBlock x:Name="ShortcutLabel" Style="{StaticResource FieldLabelStyle}" />
                    <TextBlock x:Name="ShortcutHelp" Style="{StaticResource SectionBodyStyle}" Margin="0,0,0,4" />
                    <TextBox
                        x:Name="HotkeyBox"
                        IsReadOnly="True"
                        KeyDown="HotkeyBox_KeyDown" />
                    <Button x:Name="ClearHotkeyButton" Margin="0,8,0,0" Click="ClearHotkeyButton_Click" />
                </StackPanel>

                <Expander x:Name="AdvancedExpander" IsExpanded="False">
                    <StackPanel Spacing="8">
                        <TextBlock x:Name="AdvancedHelp" Style="{StaticResource SectionBodyStyle}" />
                        <ToggleSwitch x:Name="WindowsInputDefaultsSwitch" Header="Cambiar también la entrada predeterminada de Windows para este perfil" Toggled="WindowsInputDefaultsSwitch_Toggled" />
                        <StackPanel x:Name="InputProfilePanel" Spacing="4" Visibility="Collapsed">
                            <TextBlock x:Name="InputLabel" Style="{StaticResource FieldLabelStyle}" />
                            <ComboBox x:Name="InputBox" HorizontalAlignment="Stretch" />
                            <TextBlock x:Name="InputWarning" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Visibility="Collapsed" />
                        </StackPanel>
                        <ToggleSwitch x:Name="InternalPipelineSwitch" Header="Configurar también el flujo interno" Toggled="InternalPipelineSwitch_Toggled" />
                        <StackPanel x:Name="InternalPipelinePanel" Spacing="8" Visibility="Collapsed">
                            <ToggleSwitch x:Name="ProfileInputOverrideSwitch" Header="Sobrescribir la entrada física global para este perfil" Toggled="ProfileInputOverrideSwitch_Toggled" />
                            <ToggleSwitch x:Name="UseDefaultInternalInputSwitch" Header="Seguir el micrófono predeterminado de Windows; usar la entrada guardada si el default es un cable" />
                            <TextBlock Text="Entrada que abre el motor" Style="{StaticResource FieldLabelStyle}" />
                            <ComboBox x:Name="InternalInputBox" HorizontalAlignment="Stretch" />
                            <TextBlock Text="Render del endpoint virtual final" Style="{StaticResource FieldLabelStyle}" />
                            <ComboBox x:Name="FinalVirtualRenderBox" HorizontalAlignment="Stretch" />
                            <NumberBox x:Name="EngineBufferBox" Header="Buffer (0 = Auto)" Minimum="0" Maximum="4096" SmallChange="16" SpinButtonPlacementMode="Compact" />
                            <ComboBox x:Name="InternalMuteBox" Header="Mute al activar" HorizontalAlignment="Stretch" />
                        </StackPanel>
                        <ToggleSwitch x:Name="ExternalMuteSwitch" Header="Controlar endpoints de grabación externos" Toggled="ExternalMuteSwitch_Toggled" />
                        <StackPanel x:Name="ExternalMutePanel" Spacing="8" Visibility="Collapsed">
                            <ToggleSwitch x:Name="ExternalMuteAllSwitch" Header="Aplicar a todos los endpoints de grabación activos" Toggled="ExternalMuteAllSwitch_Toggled" />
                            <ListView x:Name="ExternalMuteDevicesList" Header="Endpoints concretos" SelectionMode="Multiple" MaxHeight="180" DisplayMemberPath="Name" AutomationProperties.Name="Endpoints externos controlados" />
                            <ToggleSwitch x:Name="ForceExternalMuteSwitch" Header="Reaplicar mute si otra aplicación lo cambia" />
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                <ToggleSwitch x:Name="ExternalVolumeLockSwitch" Header="Bloquear volumen" VerticalAlignment="Bottom" />
                                <NumberBox x:Name="ExternalVolumeBox" Grid.Column="1" Header="Volumen objetivo (%)" Minimum="0" Maximum="100" Value="100" SpinButtonPlacementMode="Compact" />
                            </Grid>
                            <TextBlock Text="El gate interno sigue siendo la autoridad. Este control solo toca endpoints físicos o virtuales que el perfil elija explícitamente; los cambios externos se revisan cada segundo sin bloquear el audio." Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="Wrap" />
                        </StackPanel>
                        <ToggleSwitch x:Name="ProfilePluginChainSwitch" Header="Aplicar cadena y preset VST3" />
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock x:Name="ProfilePluginChainSummary" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" TextWrapping="Wrap" />
                            <Button Grid.Column="1" Content="Capturar cadena actual" Click="CapturePluginChainButton_Click" />
                        </Grid>
                        <ToggleSwitch x:Name="ProfileMixerSwitch" Header="Configurar mezclador" Toggled="ProfileMixerSwitch_Toggled" />
                        <StackPanel x:Name="ProfileMixerPanel" Spacing="8" Visibility="Collapsed">
                            <ComboBox x:Name="RoutingModeBox" Header="Modo de mezcla" HorizontalAlignment="Stretch" />
                            <ToggleSwitch x:Name="UseDefaultSystemCaptureSwitch" Header="Seguir la salida predeterminada de Windows; usar el endpoint guardado si el default es un cable" />
                            <ComboBox x:Name="SystemCaptureBox" Header="Endpoint de audio del sistema" HorizontalAlignment="Stretch" />
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                <NumberBox x:Name="ProfileVoiceGainBox" Header="Voz (%)" Minimum="0" Maximum="400" Value="100" SpinButtonPlacementMode="Compact" />
                                <NumberBox x:Name="ProfileSystemGainBox" Grid.Column="1" Header="PC (%)" Minimum="0" Maximum="400" Value="100" SpinButtonPlacementMode="Compact" />
                            </Grid>
                            <ToggleSwitch x:Name="ProfileDuckingSwitch" Header="Ducking al hablar" />
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                <NumberBox x:Name="ProfileDuckAmountBox" Header="Cantidad (%)" Minimum="0" Maximum="100" Value="55" SpinButtonPlacementMode="Compact" />
                                <NumberBox x:Name="ProfileDuckThresholdBox" Grid.Column="1" Header="Umbral (dBFS)" Minimum="-80" Maximum="0" Value="-36" SpinButtonPlacementMode="Compact" />
                                <NumberBox x:Name="ProfileDuckAttackBox" Grid.Column="2" Header="Ataque (ms)" Minimum="0" Maximum="5000" Value="20" SpinButtonPlacementMode="Compact" />
                            </Grid>
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                <NumberBox x:Name="ProfileDuckHoldBox" Header="Hold (ms)" Minimum="0" Maximum="10000" Value="200" SpinButtonPlacementMode="Compact" />
                                <NumberBox x:Name="ProfileDuckReleaseBox" Grid.Column="1" Header="Release (ms)" Minimum="0" Maximum="10000" Value="280" SpinButtonPlacementMode="Compact" />
                            </Grid>
                            <ToggleSwitch x:Name="ProfileProcessFilterSwitch" Header="Incluir solo las aplicaciones guardadas" />
                            <ComboBox x:Name="ProfileProcessFilterModeBox" Header="Política para aplicaciones nuevas" HorizontalAlignment="Stretch" />
                            <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <TextBlock x:Name="ProfileRulesSummary" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center" TextWrapping="Wrap" />
                                <Button Grid.Column="1" Content="Copiar selección actual del Mezclador" Click="ImportMixerRulesButton_Click" />
                            </Grid>
                            <ListView x:Name="ProfileApplicationRulesList" ItemsSource="{x:Bind ProfileApplicationRules}" SelectionMode="None" MaxHeight="240" AutomationProperties.Name="Reglas de aplicaciones del perfil">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="local:ProfileApplicationRuleRow">
                                        <Grid ColumnSpacing="8" Padding="2">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="160" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                            <CheckBox IsChecked="{x:Bind Excluded, Mode=TwoWay}" Content="Excluir" VerticalAlignment="Center" />
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{x:Bind DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                                <TextBlock Text="{x:Bind Key}" Style="{StaticResource CaptionTextBlockStyle}" TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                            <NumberBox Grid.Column="2" Header="Volumen (%)" Minimum="0" Maximum="400" Value="{x:Bind GainPercent, Mode=TwoWay}" SpinButtonPlacementMode="Compact" />
                                            <Button Grid.Column="3" Content="Quitar" Tag="{x:Bind Key}" Click="RemoveProfileRuleButton_Click" VerticalAlignment="Bottom" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                        <ToggleSwitch x:Name="AdvancedRolesSwitch" Toggled="AdvancedRolesSwitch_Toggled" />
                        <TextBlock x:Name="AdvancedRolesHint" Style="{StaticResource SectionBodyStyle}" />
                        <StackPanel x:Name="AdvancedRolesPanel" Spacing="8" Visibility="Collapsed">
                            <TextBlock x:Name="OutputConsoleLabel" Style="{StaticResource FieldLabelStyle}" />
                            <ComboBox x:Name="OutputConsoleBox" HorizontalAlignment="Stretch" />
                            <TextBlock x:Name="OutputMultimediaLabel" Style="{StaticResource FieldLabelStyle}" />
                            <ComboBox x:Name="OutputMultimediaBox" HorizontalAlignment="Stretch" />
                            <TextBlock x:Name="OutputCommunicationsLabel" Style="{StaticResource FieldLabelStyle}" />
                            <ComboBox x:Name="OutputCommunicationsBox" HorizontalAlignment="Stretch" />
                            <StackPanel x:Name="AdvancedInputRolesPanel" Spacing="8">
                                <TextBlock x:Name="InputConsoleLabel" Style="{StaticResource FieldLabelStyle}" />
                                <ComboBox x:Name="InputConsoleBox" HorizontalAlignment="Stretch" />
                                <TextBlock x:Name="InputMultimediaLabel" Style="{StaticResource FieldLabelStyle}" />
                                <ComboBox x:Name="InputMultimediaBox" HorizontalAlignment="Stretch" />
                                <TextBlock x:Name="InputCommunicationsLabel" Style="{StaticResource FieldLabelStyle}" />
                                <ComboBox x:Name="InputCommunicationsBox" HorizontalAlignment="Stretch" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Expander>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Spacing="8">
            <TextBlock x:Name="ErrorText" Foreground="{ThemeResource SystemFillColorCriticalBrush}" TextWrapping="Wrap" />
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="SaveButton" Style="{StaticResource AccentButtonStyle}" Click="SaveButton_Click" />
                <Button x:Name="CancelButton" Click="BackButton_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
